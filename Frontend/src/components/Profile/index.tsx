import { Link } from "react-router-dom";

const ProfileIcon = () => {
  return (
    <div className="mx-5">
      <Link to="profile">
        <h1>ProfileIcon</h1>
      </Link>
    </div>
  );
};

export default ProfileIcon;

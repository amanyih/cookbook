import { FontAwesomeIcon } from "@fortawesome/react-fontawesome";
import { faSearch } from "@fortawesome/free-solid-svg-icons";

const SearchBar = () => {
  return <FontAwesomeIcon icon={faSearch} className=" text-xl ml-2" />;
};

export default SearchBar;

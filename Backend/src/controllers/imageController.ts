import { Image } from "../models";

export const createImage = async (req: any, res: any) => {
  res.send("createImage");
};

export const getImage = async (req: any, res: any) => {
  res.send("getImage");
};

export const getAllImage = async (req: any, res: any) => {
  res.send("getAllImage");
};

export const updateImage = async (req: any, res: any) => {
  res.send("updateImage");
};

export const deleteImage = async (req: any, res: any) => {
  res.send("deleteImage");
};

export const getImageByRecipeId = async (req: any, res: any) => {
  res.send("getImageByRecipeId");
};
